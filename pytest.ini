[pytest]
# Self-contained: without this, pytest walks up looking for an ini file and
# picks up the unrelated parent tree's pytest.ini -- this repo is a
# standalone showcase and must not depend on that.
testpaths = tests
